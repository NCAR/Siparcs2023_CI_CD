name: matrixMult_OpenACC_Fortran_Autoconfig

on:
  schedule:
    - cron: '0 0 * * MON' 
  push:
    branches: 
      - main
    paths: 
      - Autoconfig/matrixMult/OpenACC_FORTRAN/** 
  pull_request:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  Build:
    runs-on: [self-hosted, Linux, X64, casper]
    #container:
    #  image: nvcr.io/nvidia/nvhpc:23.5-devel-cuda12.1-ubuntu22.04
    steps:
    - name: checkout
      uses: actions/checkout@v3        
    - name: Navigate to directory and make
      run: |
        cd Autoconfig/matrixMult/OpenACC_FORTRAN/ 
        pwd
        autoconf
        ./configure
        make
        echo "Test Run:"
        ./matrix_mult
